{
  "dependencies": {
    "express": "^4.21.2",
    "fabric-ca-client": "^2.2.20",
    "fabric-network": "^2.2.20",
    "lowdb": "^1.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.9",
    "supertest": "^6.3.4"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore 'db.json' --ignore '*.json' --ignore 'wallet/*' --ext js server.js",
    "test": "NODE_ENV=test jest",
    "test:watch": "NODE_ENV=test jest --watch",
    "test:coverage": "NODE_ENV=test jest --coverage"
  }
}
